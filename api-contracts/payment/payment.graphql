#import DateTime from '../date-time.graphql'
#import PaymentInput from './payment.input.graphql'
#import Payment from './payment.type.graphql'

schema {
  query: Query
  mutation: Mutation
}

type Query {
  payments: [Payment!]!
}

type Mutation {
  submitPayment(payment: PaymentInput): Payment!
}