{
  "name": "social-payments-ua",
  "version": "0.0.1",
  "engines": {
    "node": "8.11.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix back-end && npm install --prefix front-end"
  }
}
