{
  "name": "social-payments-ua",
  "version": "0.0.1",
  "engines": {
    "node": "8.11.x"
  },
  "scripts": {
    "postinstall": "npm run build:backend",
    "build:back-end": "npm run install:backend && npm run build --prefix back-end",
    "install:backend": "npm install --prefix back-end && npm install --prefix back-end --only=dev"
  }
}
